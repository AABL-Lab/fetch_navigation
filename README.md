# custom-fetch-navigation

Contains the necessary configuration and launch files for running navigation on the Fetch Robot. 

# To run with a previously generated map
roslaunch fetch_navigation freight_nav_custom.launch map_file:=/path/to/map_name.yaml
